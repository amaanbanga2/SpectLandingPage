import React from "react";

const Updated = ({ styles }) => (
  <button type="button" className={`py-2 px-4 font-poppins font-medium text-[14px] text-primary bg-blue-gradient rounded-[7px] outline-none ${styles}`}>
    Stay Updated
  </button>
);

export default Updated;
